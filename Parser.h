#pragma once

#include <vector>
#include <string>

class Parser {
private:
    std::vector<std::string> tokens;

public:

};

// vector of tokens, tree
// parseExpression() -> go through and create one node per parantheses

